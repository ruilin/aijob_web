# AI Exposure Index — Will AI take your job?

A no-backend web tool by **RL Studio**. Find your role, tune six questions to your real work, and get a task-level read on your AI exposure — what changes, what holds, and how to stay ahead.

- **Zero backend** — a single `index.html`, all logic and data run in the browser
- **210 built-in roles** with research-informed exposure scores, plus a custom fallback
- **Private by design** — nothing you enter is sent or stored anywhere

## Run locally

Just open `index.html` in a browser. No build step, no dependencies.

## Deploy to Vercel

1. Import this repo at [vercel.com/new](https://vercel.com/new)
2. Framework preset: **Other** (no build command, output directory: root)
3. Deploy — done.

Every push to `main` redeploys automatically.

## Disclaimer

Scores are directional estimates, not predictions or career advice. They reflect how much of a typical version of a role current and near-term AI can meaningfully do — how much the work *changes*, not whether the job disappears.

---

Built by RL Studio · 2026
